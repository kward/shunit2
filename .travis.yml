language: bash

env:
  - SHUNIT_COLOR='always'

script:
  - ./shunit2_test.sh

  # TODO(kward): Reenable shellcheck after all scripts have been improved.
  #- bash -c 'shopt -s globstar; shellcheck shunit2 shunit2_test_helpers *.sh'
  - shellcheck gen_test_results.sh shunit2 shunit2_test_asserts.sh
  - shellcheck -s sh shunit2_test_helpers

addons:
  apt:
    packages:
      - ksh
      - zsh
